ACCOUNTINGTEAMS_P
SQL
CREATE TABLE ACCOUNTINGTEAMS_P (
  STAFFID NUMBER,
  TEAMID NUMBER,
  T_ROLE VARCHAR2(100 BYTE),
  STATUS VARCHAR2(50 BYTE),
  REPORTID NUMBER,
  PRIMARY KEY (STAFFID),
  FOREIGN KEY (STAFFID) REFERENCES STAFF_P(STAFFID),
  FOREIGN KEY (REPORTID) REFERENCES FINANCIALREPORT_P(REPORTID)
);

ADMIN_P
SQL
CREATE TABLE ADMIN_P (
  ADMINID NUMBER GENERATED BY DEFAULT AS IDENTITY,
  PASSWORD VARCHAR2(255 BYTE),
  ROLE VARCHAR2(50 BYTE),
  STATUS VARCHAR2(50 BYTE),
  INVENTORYID NUMBER,
  REPORTID NUMBER,
  PRIMARY KEY (ADMINID),
  FOREIGN KEY (REPORTID) REFERENCES FINANCIALREPORT_P(REPORTID)
);

BILL_P
SQL
CREATE TABLE BILL_P (
  BILLID NUMBER GENERATED BY DEFAULT AS IDENTITY,
  ORDERID NUMBER,
  CUSTOMERID NUMBER,
  PAYMENTMETHOD VARCHAR2(50 BYTE),
  BILLDATE DATE,
  PAIDAMOUNT NUMBER(10,2),
  DISCOUNT NUMBER,
  PRIMARY KEY (BILLID),
  CONSTRAINT FK_BILL_ORDER FOREIGN KEY (ORDERID) REFERENCES ORDER_P(ORDERID),
  CONSTRAINT FK_BILL_CUSTOMER FOREIGN KEY (CUSTOMERID) REFERENCES CUSTOMER_P(CUSTOMERID)
);

CUSTOMER_LOGIN_P
SQL
CREATE TABLE CUSTOMER_LOGIN_P (
  CUSTOMERID NUMBER,
  LOGINDETAILS VARCHAR2(255 BYTE),
  PRIMARY KEY (CUSTOMERID),
  CONSTRAINT FK_CUSTOMER_LOGIN FOREIGN KEY (CUSTOMERID) REFERENCES CUSTOMER_P(CUSTOMERID)
);

CUSTOMER_P
SQL
CREATE TABLE CUSTOMER_P (
  CUSTOMERID NUMBER GENERATED BY DEFAULT AS IDENTITY,
  NAME VARCHAR2(100 BYTE),
  CONTACT VARCHAR2(15 BYTE),
  EMAIL VARCHAR2(100 BYTE),
  ORDERHISTORY CLOB,
  PASSWORD VARCHAR2(100 BYTE),
  PRIMARY KEY (CUSTOMERID)
);

DINNER_RESERVATIONS_P
SQL
CREATE TABLE DINNER_RESERVATIONS_P (
  ORDERID NUMBER,
  RESERVATIONID NUMBER,
  PRIMARY KEY (ORDERID, RESERVATIONID),
  CONSTRAINT FK_DINNER_RESERVATIONS_ORDER FOREIGN KEY (ORDERID) REFERENCES ORDER_P(ORDERID),
  CONSTRAINT FK_DINNER_RESERVATIONS_RESERVATION FOREIGN KEY (RESERVATIONID) REFERENCES RESERVATION_P(RESERVATIONID)
);

DINNERS_P
SQL
CREATE TABLE DINNERS_P (
  CUSTOMERID NUMBER,
  PREFERSETTINGAREA VARCHAR2(100 BYTE),
  PRIMARY KEY (CUSTOMERID),
  CONSTRAINT FK_DINNERS_CUSTOMER FOREIGN KEY (CUSTOMERID) REFERENCES CUSTOMER_P(CUSTOMERID)
);

DISH_P
SQL
CREATE TABLE DISH_P (
  DISHID NUMBER GENERATED BY DEFAULT AS IDENTITY,
  NAME VARCHAR2(100 BYTE) NOT NULL,
  INGREDIENT CLOB,
  DESCRIPTION CLOB,
  PRICE NUMBER(8,2),
  DISHPREPTIME INTERVAL DAY(2) TO SECOND(6),
  DISHTEMP VARCHAR2(50 BYTE),
  PRIMARY KEY (DISHID)
);

FEEDBACK_P
SQL
CREATE TABLE FEEDBACK_P (
  FEEDBACKID NUMBER GENERATED BY DEFAULT AS IDENTITY,
  CUSTOMERID NUMBER,
  TABLEID NUMBER,
  RESERVATIONID NUMBER,
  RESERVATIONDATE DATE,
  PRIMARY KEY (FEEDBACKID),
  CONSTRAINT FK_FEEDBACK_CUSTOMER FOREIGN KEY (CUSTOMERID) REFERENCES CUSTOMER_P(CUSTOMERID),
  CONSTRAINT FK_FEEDBACK_TABLE FOREIGN KEY (TABLEID) REFERENCES TABLE_INFO_P(TABLEID),
  CONSTRAINT FK_FEEDBACK_RESERVATION FOREIGN KEY (RESERVATIONID) REFERENCES RESERVATION_P(RESERVATIONID)
);

FINANCIALREPORT_P
SQL
CREATE TABLE FINANCIALREPORT_P (
  REPORTID NUMBER GENERATED BY DEFAULT AS IDENTITY,
  REPORTTITLE VARCHAR2(100 BYTE),
  NETPROFIT NUMBER(12,2),
  REPORTDATE DATE,
  PREPAREBY VARCHAR2(100 BYTE),
  PRIMARY KEY (REPORTID)
);

FOODAUTHORITY_P
SQL
CREATE TABLE FOODAUTHORITY_P (
  FOODAUTHID NUMBER GENERATED BY DEFAULT AS IDENTITY,
  AUTHORITYNAME VARCHAR2(100 BYTE),
  LISENCEISSUEDATE DATE,
  LISENCEENDDATE DATE,
  INSPECTIONRESULT VARCHAR2(255 BYTE),
  REMARK CLOB,
  ADMINID NUMBER,
  PRIMARY KEY (FOODAUTHID),
  FOREIGN KEY (ADMINID) REFERENCES ADMIN_P(ADMINID)
);

INVENTORY_P
SQL
CREATE TABLE INVENTORY_P (
  INVENTORYID NUMBER GENERATED BY DEFAULT AS IDENTITY,
  ITEMNAME VARCHAR2(100 BYTE),
  QUANTITY NUMBER,
  RECORDDATE DATE,
  SUPPLIERID NUMBER,
  ADMINID NUMBER,
  PRIMARY KEY (INVENTORYID),
  FOREIGN KEY (SUPPLIERID) REFERENCES SUPPLIER_P(SUPPLIERID),
  FOREIGN KEY (ADMINID) REFERENCES ADMIN_P(ADMINID)
);

KITCHENSTAFF_P
SQL
CREATE TABLE KITCHENSTAFF_P (
  STAFFID NUMBER,
  AREAOFEXPERTISE VARCHAR2(100 BYTE),
  HYGIENECERTIFICATION VARCHAR2(100 BYTE),
  KITCHENSTATION VARCHAR2(100 BYTE),
  PRIMARY KEY (STAFFID),
  FOREIGN KEY (STAFFID) REFERENCES STAFF_P(STAFFID)
);

MANAGEMENTSTAFF_P
SQL
CREATE TABLE MANAGEMENTSTAFF_P (
  STAFFID NUMBER,
  DEPARTMENT VARCHAR2(100 BYTE),
  YEAROFEXPERIENCE NUMBER,
  PERFORMANCERATING NUMBER(3,2),
  PRIMARY KEY (STAFFID),
  FOREIGN KEY (STAFFID) REFERENCES STAFF_P(STAFFID)
);

MENUITEM_P
SQL
CREATE TABLE MENUITEM_P (
  MENUITEMID NUMBER GENERATED BY DEFAULT AS IDENTITY,
  DISHID NUMBER,
  AVAILABILITY CHAR(1 BYTE) DEFAULT 'Y',
  DESCRIPTION CLOB,
  PRIMARY KEY (MENUITEMID),
  FOREIGN KEY (DISHID) REFERENCES DISH_P(DISHID)
);

ONLINE_CUSTOMER_P
SQL
CREATE TABLE ONLINE_CUSTOMER_P (
  ORDERID NUMBER,
  CUSTOMERID NUMBER,
  CARTITEM CLOB,
  DELIVERYSTATUS VARCHAR2(50 BYTE),
  DELIVERYADDRESS CLOB,
  PRIMARY KEY (ORDERID),
  CONSTRAINT FK_ONLINE_CUSTOMER FOREIGN KEY (CUSTOMERID) REFERENCES CUSTOMER_P(CUSTOMERID)
);

ORDER_DETAILS_P
SQL
CREATE TABLE ORDER_DETAILS_P (
  ORDERID NUMBER,
  DISHID NUMBER,
  QUANTITY NUMBER,
  PRICE_PER_UNIT NUMBER(10,2),
  APPROX_PREP_TIME INTERVAL DAY(2) TO SECOND(6),
  END_TIME DATE,
  START_TIME DATE,
  SERVE_STATUS VARCHAR2(50 BYTE),
  PRIMARY KEY (ORDERID, DISHID),
  CONSTRAINT FK_ORDER_DETAILS_ORDER FOREIGN KEY (ORDERID) REFERENCES ORDER_P(ORDERID),
  CONSTRAINT FK_ORDER_DETAILS_DISH FOREIGN KEY (DISHID) REFERENCES DISH_P(DISHID)
);

ORDER_P
SQL
CREATE TABLE ORDER_P (
  ORDERID NUMBER GENERATED BY DEFAULT AS IDENTITY,
  CUSTOMERID NUMBER,
  TABLEID NUMBER,
  TOTALAMOUNT NUMBER(10,2),
  ORDERDATE DATE,
  PAYMENTSTATUS VARCHAR2(50 BYTE),
  PRIMARY KEY (ORDERID),
  CONSTRAINT FK_ORDER_CUSTOMER FOREIGN KEY (CUSTOMERID) REFERENCES CUSTOMER_P(CUSTOMERID),
  CONSTRAINT FK_ORDER_TABLE FOREIGN KEY (TABLEID) REFERENCES TABLE_INFO_P(TABLEID)
);

RESERVATION_P
SQL
CREATE TABLE RESERVATION_P (
  RESERVATIONID NUMBER GENERATED BY DEFAULT AS IDENTITY,
  CUSTOMERID NUMBER,
  TABLEID NUMBER,
  RESERVATIONDATE DATE,
  TIMESLOT VARCHAR2(5 BYTE),
  PRIMARY KEY (RESERVATIONID),
  CONSTRAINT FK_RESERVATION_CUSTOMER FOREIGN KEY (CUSTOMERID) REFERENCES CUSTOMER_P(CUSTOMERID),
  CONSTRAINT FK_RESERVATION_TABLE FOREIGN KEY (TABLEID) REFERENCES TABLE_INFO_P(TABLEID)
);

SALETAX_P
SQL
CREATE TABLE SALETAX_P (
  TAXID NUMBER GENERATED BY DEFAULT AS IDENTITY,
  TAXTITLE VARCHAR2(100 BYTE),
  REPORT_DATE DATE,
  NETPROFIT NUMBER(12,2),
  PRIMARY KEY (TAXID)
);

STAFF_P
SQL
CREATE TABLE STAFF_P (
  STAFFID NUMBER GENERATED BY DEFAULT AS IDENTITY,
  S_NAME VARCHAR2(100 BYTE),
  STAFFROLE VARCHAR2(50 BYTE),
  SHIFTINGTIME DATE,
  SALARY NUMBER(10,2),
  ADMINID NUMBER,
  PRIMARY KEY (STAFFID),
  FOREIGN KEY (ADMINID) REFERENCES ADMIN_P(ADMINID)
);

SUPPLIER_P
SQL
CREATE TABLE SUPPLIER_P (
  SUPPLIERID NUMBER GENERATED BY DEFAULT AS IDENTITY,
  NAME VARCHAR2(100 BYTE),
  CONTACTDETAILS CLOB,
  RATING NUMBER(5,2),
  PRIMARY KEY (SUPPLIERID)
);

TABLE_INFO_P
SQL
CREATE TABLE TABLE_INFO_P (
  TABLEID NUMBER,
  CAPACITY NUMBER,
  BOOKING_STAT VARCHAR2(50 BYTE),
  PRIMARY KEY (TABLEID)
);

TABLE_RESERVATION_P
SQL
CREATE TABLE TABLE_RESERVATION_P (
  TABLEID NUMBER,
  RESERVATIONID NUMBER,
  RESERVATIONSTATUS VARCHAR2(50 BYTE),
  PRIMARY KEY (TABLEID, RESERVATIONID),
  FOREIGN KEY (TABLEID) REFERENCES TABLE_INFO_P(TABLEID),
  FOREIGN KEY (RESERVATIONID) REFERENCES RESERVATION_P(RESERVATIONID)
);

WAITER_P
SQL
CREATE TABLE WAITER_P (
  STAFFID NUMBER,
  ASSIGNTABLLECOUNT NUMBER,
  UNIFORMCODE VARCHAR2(50 BYTE),
  TIPSEARNED NUMBER(8,2),
  PRIMARY KEY (STAFFID),
  FOREIGN KEY (STAFFID) REFERENCES STAFF_P(STAFFID)
);